const Buttons = (props) => {

    // console.log(props)
//   const [timeRange, setTimeRange] = useState(props.hour);

  return (
    <div className="buttons">
      <button>HR</button>
      <button>D</button>
        <button>WK</button>
    </div>
  );
};

export default Buttons;